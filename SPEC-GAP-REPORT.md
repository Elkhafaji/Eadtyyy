# تنفيذ مواصفات نظام العيادة

تم فحص المشروع الحالي قبل التعديل. التطبيق الموجود هو واجهة SPA داخل `index.html` تعتمد على حالة JSON واحدة، مع API واحد (`/api/db`) يحفظ الحالة كاملة. هذا يفسر لماذا تعمل الحسابات محليًا ولا تظهر تلقائيًا على Vercel إذا لم يكن `DATABASE_URL` مضبوطًا في مشروع Vercel.

## ما تم تنفيذه في هذه المرحلة

- الإبقاء على الوظائف الحالية وعدم حذف الواجهة العاملة.
- تثبيت Node على `22.x` بدل `>=22.5.0` حتى لا تتغير نسخة التشغيل تلقائيًا.
- إعداد أساس PostgreSQL على Vercel مع تشغيل الترحيلات تلقائيًا.
- إضافة الترحيلة `003_clinic_core.sql` التي تغطي الكيانات الأساسية في المواصفات: العيادات والفروع والخدمات، المستخدمين المتخصصين، المرضى، الجداول والمواعيد والزيارات، السجل الطبي والتشخيصات والملاحظات، الأدوية والوصفات، التقييمات، المتابعات، الفواتير والمدفوعات، المحادثات والرسائل، الإشعارات والملفات، الكاونتر، وسجل التدقيق.
- إضافة مستخدم Admin وحسابات الموظفين و30 حساب مريض عند إنشاء حالة قاعدة البيانات لأول مرة. كلمة Admin الافتراضية حاليًا `Error404` إذا لم يتم ضبط `ADMIN_PASSWORD`.

## ما لم يكتمل بعد

الواجهة الحالية ما زالت تقرأ وتكتب JSON بالكامل، لذلك إضافة جداول SQL وحدها لا تضيف شاشات جديدة تلقائيًا. يلزم في المرحلة التالية نقل كل عملية إلى API مستقل بصلاحيات Server-side، ثم ربط الشاشات تدريجيًا: الحجز العام، المرضى، الجدول والانتظار، السجل الطبي والوصفات، الدفع، المعمل، الأشعة، الإجراءات، الفروع، الرسائل، الإشعارات والتقارير.

لن يتم وضع بيانات تجريبية داخل الجداول الجديدة، ولن يتم عرض أزرار لا تنفذ عملية حقيقية. الحسابات المزروعة في حالة JSON هي فقط حسابات الدخول المطلوبة للتشغيل الأول.

## المطلوب للنشر

1. رفع `api/db.js` و`package.json` و`vercel.json` ومجلد `migrations` كاملًا إلى نفس المستودع.
2. ربط قاعدة Neon/Postgres بمشروع Vercel الجديد، مع Production وPreview، والتأكد من وجود `DATABASE_URL`.
3. إعادة النشر ثم فتح `/api/db` مرة واحدة؛ عند نجاحه ستُنشأ الترحيلات والحسابات تلقائيًا.
4. بعد ذلك تبدأ مرحلة ربط الواجهة بالـ API الجديد. لا يمكن اعتبار المواصفات كاملة قبل هذه المرحلة.
